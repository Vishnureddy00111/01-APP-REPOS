name: Cart CI Pipeline
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - users: actions/checkout@v4
      - run: |
          echo

  tests:
   runs-on: self-hosted
   needs: build
   steps:
      - uses: actions/checkout@v4
      - run: |
          echo

  Dev:
   runs-on: self-hosted
   needs:
   steps:
    - uses: actions/checkout@v4
    - run: |
        echo